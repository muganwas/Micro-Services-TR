{
	"name": "micro-services",
	"version": "1.0.0",
	"scripts": {
		"orders": "cd orders && docker-compose up -d",
		"products": "cd products && docker-compose up -d",
		"distance": "cd distance && docker-compose up -d",
		"api-gateway": "cd api-gateway && node server.js",
		"start": "npm run orders && npm run products && npm run distance && npm run api-gateway"
	}
}
